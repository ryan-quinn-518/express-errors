///////////////////////////////////////////////////////////////////////////////
// Imports
///////////////////////////////////////////////////////////////////////////////
import {
    HTTP_ERROR_NAMES,
    HttpError,

    type ExpressMiddleware,
    type HttpErrorName,
    type ProblemDetails,
} from "./types.js";

import {
    createGlobalErrorHandler,
} from "./fn.js";

///////////////////////////////////////////////////////////////////////////////
// Exports
///////////////////////////////////////////////////////////////////////////////
export type {
    ExpressMiddleware,
    HttpErrorName,
    ProblemDetails,
};

export {
    HTTP_ERROR_NAMES,

    HttpError,

    createGlobalErrorHandler,
};
